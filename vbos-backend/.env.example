# Required for all environments
DJANGO_SECRET_KEY="local"

# Database - only needed if not using default docker-compose postgres
# DJANGO_DB_URL="postgis://postgres:postgres@postgres:5432/vbos"

# DigitalOcean Spaces (S3 compatible) - for production file storage
# Leave empty for local dev (files stored in ./media/)
# Get credentials from: DigitalOcean > Spaces > Create Space > Access Keys
DJANGO_AWS_ACCESS_KEY_ID=""
DJANGO_AWS_SECRET_ACCESS_KEY=""
DJANGO_AWS_STORAGE_BUCKET_NAME=""

# Optional: Custom S3 endpoint (default: https://syd1.digitaloceanspaces.com)
# DJANGO_AWS_S3_ENDPOINT_URL="https://syd1.digitaloceanspaces.com"
